# Docusaurus 3 has known incompatibilities with Yarn PnP — internal modules
# require() the host package.json from ESM context, which triggers
# ERR_INTERNAL_ASSERTION on recent Node releases. Use the node_modules linker.
nodeLinker: node-modules

# Tell Yarn to write a real lockfile and a real node_modules tree.
enableGlobalCache: true
